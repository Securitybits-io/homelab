plugin_id   = "nfs"
type        = "csi"
id          = "immich-data"
name        = "Immich Data"

capability {
	access_mode = "multi-node-multi-writer" # multi-node, both api server and worker tasks
	attachment_mode = "file-system"
}

context {
  server = "10.0.11.241"
  share = "/Securitybits.Systems/Immich/immich_data"
  subDir = "immich"
}

mount_options {
  fs_type = "nfs"
  mount_flags = [ "nolock", "soft", "async", "nfsvers=3" ]
}