---
# tasks file for ytdl
  # - name: install YT-DLP dependencies
  #   apt:
  #     name: '{{ ytdl_dependencies }}'
  #     state: present

  # - name: install YT-DLP
  #   pip:
  #     name: yt-dlp
  #     state: present

  # - name: create channel file
  #   copy:
  #     dest: "/opt/channel.list"
  #     content: |
  #       # {{ item.channel }}
  #       {{ item.url }}
  #   loop: '{{ channels }}'

  - name: create channel file
    template:
      src: channel.list
      dest: "/opt/channel.list"